import { BaseForLines, IGameCode } from './BaseForLines';
import { GameSprite, IGameTile } from './tile';
export declare class CollisionLayer extends BaseForLines {
    readonly id: number;
    private sprites;
    constructor(source: IGameCode, tiles: IGameTile[]);
    getBitSetIndexOf(sprite: GameSprite): number;
}
