import { RULE_DIRECTION_WITH_RELATIVE } from '../util';
import * as ast from './astTypes';
export declare enum ValidationLevel {
    ERROR = 0,
    WARNING = 1,
    INFO = 2
}
declare class Parser {
    private grammar;
    constructor();
    parseToAST(code: string): ast.IASTGame<RULE_DIRECTION_WITH_RELATIVE, string, string, number | ".">;
    parse(code: string): {
        data: import("..").GameData;
    };
}
declare const _default: Parser;
export default _default;
